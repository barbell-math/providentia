# _deps

Dependencies that providentia links against in some way are stored here so that
the providentia library can be downloaded and immediately be used without
needing to download a bunch of other dependencies.
